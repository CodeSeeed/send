# API base URL (defaults to /api when empty)
# When using Vite dev server, the proxy in vite.config.ts forwards /api to the Go backend.
# In production, set this to the API server URL if it differs from the frontend origin.
# VITE_APP_BASE_API=/api